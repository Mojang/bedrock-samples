{
  "$id": "1557623393",
  "$schema": "http://json-schema.org/draft-07/schema#",
  "definitions": {
    "4004946602": {
      "oneOf": [
        {
          "items": {
            "type": "number"
          },
          "maxItems": 3,
          "minItems": 3,
          "type": "array"
        },
        {
          "properties": {
            "x": {
              "type": "number"
            },
            "y": {
              "type": "number"
            },
            "z": {
              "type": "number"
            }
          },
          "type": "object"
        }
      ],
      "title": "vec3"
    }
  },
  "description": "Allows the entity to first travel to a random point on the outskirts of the village, and then explore random points within a small distance. This goal requires \"minecraft:dweller\" and \"minecraft:navigation\" to execute.",
  "properties": {
    "control_flags": {
      "items": {
        "enum": [
          "move",
          "look",
          "jump"
        ],
        "title": "Goal's control flags",
        "type": "string"
      },
      "type": "array"
    },
    "dist_from_boundary": {
      "$ref": "#/definitions/4004946602",
      "description": "The distance from the boundary the villager must be within in to explore the outskirts. Default value: Vec3(5,0,5)."
    },
    "explore_dist": {
      "description": "Total distance in blocks the the entity will explore beyond the village bounds when choosing its travel point. Default value: 5.00.",
      "type": "number"
    },
    "max_travel_time": {
      "description": "This is the maximum amount of time an entity will attempt to reach it's travel point on the outskirts of the village before the goal exits. Default value: 60.0.",
      "type": "number"
    },
    "max_wait_time": {
      "description": "The wait time in seconds between choosing new explore points will be chosen on a random interval between this value and the minimum wait time. This value is also the total amount of time the entity will explore random points before the goal stops. Default value: 0.0.",
      "type": "number"
    },
    "min_dist_from_target": {
      "description": "The entity must be within this distance for it to consider it has successfully reached its target. Default value: 2.2.",
      "type": "number"
    },
    "min_perimeter": {
      "description": "The minimum perimeter of the village required to run this goal. Default value: 1.0.",
      "type": "number"
    },
    "min_wait_time": {
      "description": "The wait time in seconds between choosing new explore points will be chosen on a random interval between this value and the maximum wait time. Default value: 3.0.",
      "type": "number"
    },
    "next_xz": {
      "description": "A new explore point will randomly be chosen within this XZ distance of the current target position when navigation has finished and the wait timer has elapsed. Default value: 5.",
      "type": "integer"
    },
    "next_y": {
      "description": "A new explore point will randomly be chosen within this Y distance of the current target position when navigation has finished and the wait timer has elapsed. Default value: 3.",
      "type": "integer"
    },
    "priority": {
      "minimum": 0,
      "type": "integer"
    },
    "speed_multiplier": {
      "description": "The multiplier for speed while using this goal. 1.0 maintains the speed. Default value: 1.0.",
      "type": "number"
    },
    "timer_ratio": {
      "description": "Each new explore point will be chosen on a random interval between the minimum and the maximum wait time, divided by this value. This does not apply to the first explore point chosen when the goal runs. Default value: 2.0.",
      "type": "number"
    }
  },
  "title": "minecraft:behavior.explore_outskirts",
  "type": "object",
  "x-format-version": "1.21.130"
}